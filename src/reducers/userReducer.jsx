import { Map } from 'immutable'


const defaultState = Map({
    
})

export default (state = defaultState, { type, payload }) => {
    switch (type) {
        default:
            return state
    }

}
